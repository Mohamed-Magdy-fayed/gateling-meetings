services:
  db:
    image: postgres:17.0
    container_name: gateling_meetings_db
    hostname: gateling_meetings_db
    ports:
      - "5434:5432"
    environment:
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_DB=${DB_NAME}
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
    name: gateling_meetings_pgdata
